#include "main.h"

/**
 * _puts - function that prints a string, followed by a new line
 * @str: the string
 */

void _puts(char *str)
{
	int i = 0;

	while (s[i])
	{
		_putchar(s[i]);
		i++;
	}
	_putchar('\n');
}
